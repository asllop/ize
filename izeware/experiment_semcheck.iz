import EventData as Data from izeware.models.event_data
import EventData from izeware.models.event_data_bis

model Event (
    msg as "message": Str,
    ts: Timestamp,
    data: Data,
    data_bis: EventData
)

model Timestamp Int

transfer MyTransfer in:Event -> Str
    in.data.type.Inner().Type().ToStr()