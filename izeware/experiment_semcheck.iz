import EventData as Data from izeware.models.event_data
import EventData, APIFY_URL from izeware.models.event_data_bis

model Event (
    msg as "message": Str,
    ts: Timestamp,
    data: Data,
    data_bis: EventData
)

model Timestamp Int

transfer my_transfer in:Event -> Str
    //in.data.ev_type.inner().type().to_str()
    "Hello"